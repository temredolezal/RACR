	<% @page_title = "New Section" %>
	<br>
	<div class="links btn-group">
		<%= link_to("<< Back to Project List", {:action => 'index', :project_id => @project.id}, :class => 'btn btn-custom') %>
	</div>

	  <h2>Create Section</h2>

	  <%= form_for(:section, :url => {:action => 'create', :project_id => @project.id}) do |f| %>

	    <%= render(:partial => "form", :locals => {:f => f}) %>
	    <br>

	    <div class="links btn-group">
		    <%= submit_tag("Create Section", :class => 'btn btn-custom') %>
		</div>

	  <% end %>
