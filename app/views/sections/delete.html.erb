	<% @page_title = "Delete Section" %>

	<div class="links btn-group">
		<%= link_to("<< Back to Section List", {:action => 'index', :project_id => @project.id}, :class => 'btn btn-custom') %>
	</div>

	  <h2>Delete Section</h2>

	  <%= form_for(:section, :url => {:action => 'destroy', :id => @section.id, :project_id => @project.id}) do |f| %>

	    <h3>Are you sure you want to permanently delete "<%= @section.name %>"Section?</h3>

	    <div class="links btn-group">
	    	<%= submit_tag("Yes, Delete Section", :class => 'btn btn-custom') %>
	    </div>

	  <% end %>